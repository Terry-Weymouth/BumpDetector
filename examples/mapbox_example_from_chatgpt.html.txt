<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Mapbox GL JS Example</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src="https://api.mapbox.com/mapbox-gl-js/v2.6.1/mapbox-gl.js"></script>
  <link href="https://api.mapbox.com/mapbox-gl-js/v2.6.1/mapbox-gl.css" rel="stylesheet">
  <style>
    body { margin: 0; padding: 0; }
    #map { position: absolute; top: 0; bottom: 0; width: 100%; }
    .marker {
      background-color: black;
      border-radius: 50%;
      width: 10px;
      height: 10px;
      border: 2px solid white; /* Optional: Add a white border around the dot */
    }
  </style>
</head>
<body>

<div id="map"></div>

<script>
  // Your Mapbox access token
  mapboxgl.accessToken = 'pk.eyJ1Ijoid2V5bW91dGgiLCJhIjoiY2phNjFwcWx6M3RzajJ3bnJycTE1dG90NiJ9._9zGTEu12KL8CZIrhsTooQ';

  // Your array of points
  var points_long_lat = [
    [-73.985428, 40.748817],  // Example point (longitude, latitude)
    // Add more points as needed
  ];

  // Create a map centered at the first point
  var map = new mapboxgl.Map({
    container: 'map',
    style: 'mapbox://styles/mapbox/streets-v11',
    center: points_long_lat[0],
    zoom: 12
  });

  // Add markers for each point
  points_long_lat.forEach(function(point) {
    // Create a DOM element for each marker
    var el = document.createElement('div');
    el.className = 'marker';

    // Create the marker
    new mapboxgl.Marker(el)
      .setLngLat(point)
      .addTo(map);
  });
</script>

</body>
</html>
